Prevent invisible project images from loading when page loads
Push Slartichook to Github
Namespace JS
Prevent caching when DEBUG = True

Design:
  Extend "about" vertical border
  Blog section
  Ensure header is right-aligned
  Text shadows on headers
  Place border around project image viewport
